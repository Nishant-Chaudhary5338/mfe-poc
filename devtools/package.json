{
  "name": "devtools",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --names SERVER,CLIENT --prefix-colors cyan,magenta \"node server.js\" \"pnpm --filter devtools-client dev\"",
    "server": "node server.js",
    "client": "pnpm --filter devtools-client dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
